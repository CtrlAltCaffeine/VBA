import streamlit as st

def main():
    st.title("Home")
    user_type = st.selectbox("Select User Type", ["Select", "Viewer", "Authenticator"], label_visibility="off")
    
    if st.button('Submit'):
        if user_type == "Viewer":
            st.experimental_set_query_params(url="Viewer")
        elif user_type == "Authenticator":
            st.experimental_set_query_params(url="Authenticator")
        else:
            st.warning('Please select a user type!')

def viewer_page():
    st.title("Viewer Page")
    st.write("""
    The Taj Mahal is an ivory-white marble mausoleum on the right bank of the Yamuna river in the Indian city of Agra.
    It was commissioned in 1632 by the Mughal emperor Shah Jahan to house the tomb of his favourite wife, Mumtaz Mahal.
    """)

def authenticator_page():
    st.title("Authenticator Page")
    st.write("""
    The Eiffel Tower is a wrought-iron lattice tower on the Champ de Mars in Paris, France.
    It is named after the engineer Gustave Eiffel, whose company designed and built the tower.
    """)

def app():
    url_path = st.experimental_get_query_params().get('url', ['Home'])[0]

    if url_path == 'Home':
        main()
    elif url_path == 'Viewer':
        st.subheader("Viewer")
        viewer_page()
    elif url_path == 'Authenticator':
        st.subheader("Authenticator")
        authenticator_page()

if __name__ == "__main__":
    app()
